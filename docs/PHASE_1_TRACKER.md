# GuardAI Phase 1 Tracker — Safe Foundation

> Active implementation tracker for the foundation work in `GUARDAI_MASTER_BUILD_GUIDE.md`.

## Status

**PHASE 1: IN PROGRESS — repository implementation is advanced; clean execution remains externally blocked by GitHub Actions billing/spending state.**

No install, lint, typecheck, build, migration or test result is marked green until it actually executes in a clean environment.

---

## Current baseline

| Item | Current GuardAI decision |
|---|---|
| Node | `24.18.1` LTS |
| Frontend | React 19 + TypeScript + Vite 8 |
| Backend | Node/Express CommonJS, modular |
| Application API | `/api/v1/...` |
| Operational health | `/api/health` |
| Shared scan contract | `0.2.0` |
| Auth / DB | dedicated GuardAI Supabase Auth + Postgres project |
| Backend DB access | native PostgreSQL repositories/transactions, ADR 0002 |
| Worker model | separate processes; first real worker = `security` |
| Existing connected Supabase project | **must never be reused for GuardAI** |
| Legacy free prototype endpoints | disabled by default; explicit local-development opt-in only |

---

# Repository / development standards

- [x] runtime/package-manager baseline
- [x] `.nvmrc`, `.editorconfig`, contribution rules
- [x] hardened `.gitignore`
- [x] generated npm cache trees removed
- [x] frontend quality scripts
- [x] recursive backend JavaScript syntax-check source script
- [x] GitHub Actions workflow registered
- [x] full-history Gitleaks configured
- [x] external GitHub Actions pinned to immutable SHAs
- [x] stale/incomplete backend lockfile removed instead of trusted
- [ ] backend lockfile regenerated by verified clean install
- [ ] root clean install executed
- [ ] backend clean install executed
- [ ] lint/typecheck/build executed
- [ ] backend syntax/tests executed

GitHub created a real Actions run previously, but allocated no runner (`runner_id = 0`, no steps) because of the account billing/spending condition. That failure is not treated as a GuardAI code failure.

---

# Frontend integrity / truthfulness

Completed:

- [x] duplicate App rendering removed
- [x] typed central navigation
- [x] global Error Boundary
- [x] truthful landing/footer/result/report wording
- [x] legacy product mocks isolated behind `FeaturePreview`
- [x] no fake-success network/API fallback
- [x] typed prototype scan options
- [x] Accessibility shown as unavailable until a real engine exists
- [x] evidence-first current result view
- [x] missing coverage shown as `Nicht bewertet`
- [x] fake industry benchmark removed from active UI
- [x] backend coverage notices rendered
- [x] persistent workspace DTO types prepared
- [x] authenticated API-client core prepared without adding a new package
- [x] frontend workspace API prepared for Organizations, Targets, DNS verification, persistent Scan submission and Scan status
- [x] persistent responses reject an incompatible `0.2.0` scan contract

Still required:

- [ ] dedicated GuardAI Supabase project exists
- [ ] clean install permits adding/locking `@supabase/supabase-js`
- [ ] real frontend login/session provider
- [ ] Workspace/Target/Verification UI
- [ ] active scanner UI migrated from legacy synchronous prototype flow to persistent authenticated flow
- [ ] legacy frontend `ScanResult` compatibility fields removed

---

# API / contract boundary

- [x] shared metadata source: `shared/scan-contract.json`
- [x] current contract version: `0.2.0`
- [x] legacy prototype response validation remains centralized
- [x] persistent Scan submission/status response schemas added
- [x] persistent public response strips internal fields such as worker IDs
- [x] frontend checks persistent contract compatibility
- [x] structured API error envelope: `error.code`, `error.message`, optional `error.details`
- [x] rate-limit/validation/upload/404/HttpError paths use structured errors
- [x] application routes live under `/api/v1`
- [x] persistent contract declares Scan status, Job status, module IDs, coverage states, Finding severity and target verification states
- [ ] Organization/Target general API schemas promoted to a broader API contract if external SDK publication requires it
- [ ] native detector assessment model replaces remaining legacy frontend score/status compatibility types

---

# Backend modular architecture

`server/index.js` is process bootstrap only. Active responsibilities are split across:

```text
server/
├── index.js
├── app.js
├── config.js
├── runtime.js
├── auth/
├── database/
├── domain/
├── lib/
├── middleware/
├── repositories/
├── routes/
├── scanners/
├── services/
├── scripts/
├── workers/
└── test/
```

API serving, target verification, authorization, persistence, scanner execution and worker lifecycle are separate layers.

---

# Scanner / network / upload security

## Scanner truthfulness

- [x] requested prototype modules are enforced
- [x] no fake Accessibility score
- [x] fabricated GitHub repository score disabled
- [x] AI outputs schema-validated
- [x] arbitrary model-provided scores ignored
- [x] webpage/document content treated as untrusted prompt data
- [x] anonymous paid-AI access disabled by default
- [x] persistent pipeline refuses any module without a real worker
- [x] currently only persistent `security` is externally enabled

## SSRF / target safety

- [x] HTTP/HTTPS only
- [x] embedded URL credentials rejected
- [x] nonstandard ports rejected
- [x] private/loopback/link-local/reserved IPv4 blocked
- [x] risky/mapped IPv6 ranges blocked conservatively
- [x] every redirect target revalidated
- [x] environment proxy routing disabled for scanner targets
- [x] target request timeout/size bounded
- [x] socket-level DNS address validation
- [x] validated final URL exposed to detectors
- [x] regression test sources for URL/IP/DNS/socket behavior

## Security header detector

- [x] detector ID/version: `security.headers` / `1.0.0`
- [x] final HTTPS transport check
- [x] CSP presence check
- [x] HSTS only assessed on HTTPS final target
- [x] X-Frame-Options / CSP `frame-ancestors` detection
- [x] raw HTTP headers are not persisted
- [x] normalized Evidence payload
- [x] synchronous dev prototype and persistent worker share detector logic
- [x] detector test sources

## File boundary

- [x] one file, max 10 MB
- [x] PDF/TXT only
- [x] extension + MIME checks
- [x] PDF signature check
- [x] binary/null-byte TXT rejection
- [x] temporary file cleanup
- [x] fake image/presentation extraction removed
- [x] `pdf-parse 2.4.5` v2 lifecycle with `destroy()`
- [ ] malware quarantine
- [ ] parser sandbox / stronger process/resource isolation
- [ ] public document worker remains disabled until these gates exist

---

# Legacy prototype isolation

The old synchronous endpoints do not provide SaaS target ownership verification and therefore cannot be the production surface.

Implemented:

- [x] `ALLOW_PROTOTYPE_SCAN_ENDPOINTS=false` fail-safe default
- [x] `/api/v1/scan` blocked unless explicitly enabled for controlled development
- [x] `/api/v1/scan-file` blocked before Multer/disk write when disabled
- [x] separate anonymous Gemini gate remains fail-safe

Production direction: remove the legacy endpoints after the authenticated frontend has fully migrated to the persistent pipeline.

---

# Dedicated GuardAI database design

Architecture decisions:

- `docs/adr/0001-dedicated-supabase-postgres-auth.md`
- `docs/adr/0002-native-postgres-backend-transactions.md`

Current SQL design sources — **none are applied migrations**:

1. `database/001_guardai_core_schema_draft.sql`
2. `database/002_scan_queue_invariants_draft.sql`
3. `database/003_worker_result_invariants_draft.sql`
4. `database/004_target_verification_challenges_draft.sql`
5. `database/005_workspace_onboarding_invariants_draft.sql`
6. `database/006_entitlements_usage_draft.sql`

Design currently covers:

- [x] profiles
- [x] organizations / memberships
- [x] targets
- [x] scans / scan_jobs
- [x] Evidence
- [x] rules / rule_versions / legal_sources
- [x] Findings / Finding instances
- [x] subscriptions / audit events
- [x] RLS design
- [x] private authorization helper design
- [x] browser read-mostly model
- [x] composite tenant FKs
- [x] organization-scoped Scan idempotency
- [x] unique Scan/module jobs
- [x] worker result/completion/failure invariants
- [x] Evidence/Finding duplicate-prevention invariants
- [x] backend-only DNS target verification challenges
- [x] duplicate Target prevention per organization/type/canonical URL
- [x] plan entitlements + durable monthly usage/reservation model
- [ ] dedicated GuardAI Supabase project provisioned
- [ ] drafts consolidated into a generated migration
- [ ] migration applied to staging
- [ ] Supabase security/performance advisors executed
- [ ] cross-tenant/RLS/queue/usage integration tests executed

---

# Authentication / RBAC / Workspace onboarding

## Auth foundation

- [x] `SUPABASE_URL` + publishable-key server boundary
- [x] no service-role/secret key intended for browser
- [x] Bearer parsing tests
- [x] fail-closed token validation through Supabase Auth user endpoint
- [x] `/api/v1/auth/me`
- [x] owner/admin/member/viewer role hierarchy
- [x] authorization service + tests
- [x] membership repository

## Organization lifecycle

Prepared authenticated routes:

```text
GET  /api/v1/organizations
POST /api/v1/organizations
```

Implemented:

- [x] organization name normalization
- [x] URL-safe random-suffixed slug generation
- [x] slug collision retry boundary
- [x] Organization + owner Membership created atomically
- [x] initial internal/free subscription state in same transaction
- [x] `organization.created` audit event in same transaction
- [x] organization listing scoped to authenticated memberships

## Website Target lifecycle

Prepared routes:

```text
GET  /api/v1/organizations/:organizationId/targets
GET  /api/v1/organizations/:organizationId/targets/:targetId
POST /api/v1/organizations/:organizationId/targets
```

Implemented:

- [x] create requires `admin+`
- [x] read/list requires `viewer+`
- [x] website target only for current onboarding surface
- [x] bare domains normalized to HTTPS
- [x] query/fragment removed from stable Target identity
- [x] local/IP hostnames rejected for DNS-verifiable SaaS Targets
- [x] credentials/nonstandard ports rejected
- [x] Target always starts `unverified`
- [x] client cannot set verification state
- [x] duplicate canonical Target rejected
- [x] `target.created` audit event

---

# Target ownership verification

Prepared admin-only routes:

```text
POST /api/v1/organizations/:organizationId/targets/:targetId/verification-challenges
POST /api/v1/organizations/:organizationId/targets/:targetId/verification-challenges/check
```

Implemented:

- [x] DNS TXT method for website Targets
- [x] 256-bit random token
- [x] record name `_guardai-challenge.<hostname>`
- [x] record value `guardai-verification=<token>`
- [x] only SHA-256 token hash persisted
- [x] raw token returned only when creating the challenge
- [x] timing-safe hash comparison
- [x] segmented DNS TXT records supported
- [x] 30-minute challenge expiry design
- [x] max 20 verification attempts
- [x] one pending challenge per Target
- [x] separate verification rate limit
- [x] only successful DB transaction can set Target `verified`
- [x] persistent Scan submission requires `verified`
- [x] worker execution rechecks that verification is still valid
- [x] service tests use fake DNS and do not require network access
- [ ] periodic production re-verification policy
- [ ] real staging DNS integration test

---

# Native Postgres repositories

Current server-only DB configuration:

```text
DATABASE_URL
DATABASE_POOL_MAX
```

Repository layer includes:

- [x] bounded Postgres pool
- [x] Membership repository
- [x] Organization repository
- [x] Target repository
- [x] Target verification repository
- [x] Scan submission repository
- [x] Scan status/read repository
- [x] Job repository
- [x] Entitlement/usage reservation repository
- [x] parameterized SQL
- [x] fail-closed missing DB configuration

`pg 8.22.0` is selected. Backend lockfile/install verification remains pending.

---

# Persistent Scan lifecycle

## Submission

```text
POST /api/v1/organizations/:organizationId/targets/:targetId/scans
```

- [x] authenticated user
- [x] organization role `member+`
- [x] Target belongs to Organization
- [x] Target must be verified
- [x] Target/module compatibility
- [x] only implemented modules accepted
- [x] currently enabled module = `security`
- [x] atomic Scan + Job transaction
- [x] organization-scoped `Idempotency-Key`
- [x] concurrent idempotency conflict handling
- [x] same key/different request → `409 IDEMPOTENCY_KEY_REUSED`
- [x] 202 creation / 200 idempotent replay

## Job leasing

- [x] job-type filter
- [x] `available_at` scheduling
- [x] expired lease reclaim
- [x] attempt limit
- [x] `FOR UPDATE SKIP LOCKED`
- [x] bounded worker IDs / lease durations
- [x] lease ownership before execution/result write
- [x] lease renewal method
- [x] first claim moves Scan queued → running

## Completion / Evidence / Findings

- [x] worker assessment validation
- [x] canonical Evidence SHA-256
- [x] deterministic Finding fingerprint
- [x] Evidence persistence in completion transaction
- [x] Finding upsert + per-Scan instance
- [x] duplicate completion protection
- [x] Job result summary/completion timestamp
- [x] per-module coverage
- [x] overall score from completed module result summaries
- [x] all jobs complete → Scan completed

## Failure / retry

- [x] bounded exponential backoff
- [x] retry attempt limit
- [x] permanent errors classified separately
- [x] permanent error immediately fails Scan
- [x] retry exhaustion fails Scan
- [x] remaining sibling jobs cancelled after terminal failure
- [x] retry coverage state
- [x] persisted error text normalized/bounded

## Read model

```text
GET /api/v1/organizations/:organizationId/scans/:scanId
```

- [x] authenticated `viewer+`
- [x] tenant-scoped Scan
- [x] user-safe Jobs
- [x] result summaries/errors
- [x] normalized Evidence
- [x] Finding instances
- [x] worker IDs excluded
- [x] response validated against persistent `0.2.0` contract

---

# First real worker

Security worker:

```text
npm run worker:security
npm run worker:security:once
```

Implemented:

- [x] separate process from API
- [x] only claims `security`
- [x] safe final-URL-aware HTTP fetch
- [x] normalized security-header Evidence
- [x] completion/failure integration
- [x] retry/permanent-error policy integration
- [x] graceful SIGTERM/SIGINT loop shutdown
- [x] DB pool cleanup
- [ ] heartbeat loop for future long-running modules; current security timeout remains below default lease
- [ ] production process manager/container deployment

---

# Entitlements / durable usage

Commercial plan numbers are intentionally **not invented** in code.

Implemented design/code:

- [x] plan capability model
- [x] module→capability mapping
- [x] `security` currently has no paid capability requirement
- [x] Privacy/AI-Governance → `ai_screening`
- [x] Accessibility → `browser_scan`
- [x] Repository → `repository_scan`
- [x] Asset → `ai_document_screening`
- [x] missing/disabled entitlement fails closed
- [x] monthly limit includes concurrent reserved usage
- [x] durable monthly usage counters
- [x] Scan-scoped Usage reservations
- [x] deterministic lock order for multiple capabilities
- [x] idempotent reservation lookup
- [x] reservation finalize as consumed/released
- [x] browser roles receive no direct mutation rights to usage tables
- [ ] approved plan/capability limits seeded
- [ ] paid worker submission wired to mandatory reservation at activation time
- [ ] billing provider lifecycle connected

---

# Current test-source coverage

Node test sources currently cover or prepare coverage for:

- target/IP/DNS/socket safety
- prototype access policy
- anonymous AI gate
- scan contracts
- persistent scan contract
- API error envelope
- auth header parsing
- organization roles/authorization
- Organization normalization/service
- Website Target normalization/service
- target/module compatibility
- target verification requirement
- DNS challenge rules/service
- Scan lifecycle/submission
- idempotency helpers
- worker lifecycle/retry policy
- Job lease guards
- assessment result validation
- Evidence hashing/Finding fingerprints
- security-header detector semantics
- entitlement/capability rules
- usage reservation input rules

**These test files have not yet been executed in a clean validated environment.**

---

# Current blockers

1. GitHub Actions still cannot allocate a runner because of the account billing/spending condition.
2. Backend dependency installation has not been rerun after package changes; the backend lockfile remains intentionally absent.
3. Dedicated GuardAI Supabase project has not been provisioned.
4. Therefore migration/RLS/Auth/DB/worker integration tests cannot yet run against a real GuardAI staging environment.
5. Root dependency installation cannot yet safely add and lock the Supabase frontend client.
6. Active visual scanner still uses the legacy prototype client; those backend endpoints are now disabled by default until explicitly enabled for controlled development.

---

# Phase 1 exit criteria

- [x] repository/runtime standards
- [x] modular backend foundation
- [x] versioned API + structured errors
- [x] scanner SSRF/upload safety foundation
- [x] dedicated DB/Auth architecture
- [x] DB/RLS design sources
- [x] Auth/RBAC foundation
- [x] Organization/Target backend onboarding path
- [x] DNS Target ownership verification foundation
- [x] native Postgres repositories
- [x] persistent Scan lifecycle
- [x] durable worker lifecycle
- [x] first real Security worker
- [x] price-neutral entitlement/usage foundation
- [x] frontend persistent workspace API boundary prepared
- [ ] root `npm ci` verified
- [ ] backend clean install + lockfile verified
- [ ] frontend lint/typecheck/build verified
- [ ] backend syntax/tests verified
- [ ] dedicated staging DB migration/RLS integration verified

---

# Next implementation order

While execution/cloud provisioning remains blocked or intentionally deferred:

1. synchronize repo inventory/master guide with the new onboarding/worker/entitlement architecture,
2. prepare frontend Auth/session state behind an adapter without pretending Supabase is configured,
3. prepare Workspace/Target/Verification UI states against the new persistent API,
4. design production deployment/process topology for API + Security worker + database,
5. implement browser/axe Accessibility worker only after its dependency/runtime can be cleanly installed and locked,
6. implement document malware quarantine/parser isolation before any public Asset worker,
7. provision a **dedicated GuardAI staging Supabase project** when cloud provisioning is deliberately reached,
8. immediately prioritize real clean install/build/test/migration validation when the GitHub runner or equivalent environment becomes available.
